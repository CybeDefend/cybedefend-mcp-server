# Environment variables for tests
# Note: These values are fake URLs because tests don't make real HTTP calls
# They only test tool structure and MCP protocol compliance
API_BASE=https://mock.cybedefend.test
